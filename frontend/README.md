# Frontend placeholder

This static frontend is a minimal HTML page that posts text to the backend endpoint `/api/embeddings`.

If you prefer a full Angular app, you can replace the contents of this directory with an Angular project and update the Dockerfile accordingly.
